---
  - name: "check info"
    hosts: all
    connection: local 
    tasks:

    - name: "get ipaddress"
      shell: "ifconfig"
      register: "output"
      delegate_to: localhost

    - debug: var=output.stdout_lines
